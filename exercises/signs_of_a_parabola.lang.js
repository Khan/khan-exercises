({
	"nl" : {
		"question1"	: '<code>a\cdot x^2 + b\cdot x + c</code> is graphed below. Determine the signs of <code>a</code>, <code>b</code>, and <code>c</code>.',
		
		"hint1"		: 'The number <code>a</code> determines how the legs are oriented. Is the parabola smiling or frowning?',
		"hint2"		: 'A smiling parabola means <code>a</code> is positive and a frowning parabola means <code>a</code> is negative.',
		"hint3"		: 'The parabola is <var>( A > 0) ? "smiling" : "frowning"</var> thus <code>a</code> is <var>( A > 0 ) ? "positive" : "negative"</var>.',
		"hint4"		: 'The number <code>c</code> determines where the parabola intersects the <code>y</code>-axis. Is the positive or negative part?',
		"hint5"		: 'If the parabola intersected the positive part of the <code>y</code>-axis, then <code>c</code> would be positive.',
		"hint6"		: "The parabola intersects the <code>y</code>-axis in the point <code>(0,c) = (0,<var>C</var>)</code>, thus <code>c</code> is <var>( C > 0 ) ? 'positive' : ( (C<0) ? 'negative' : 'zero' )</var>.",
		"hint7"		: 'The number <code>b</code> determines how the parabola intersects the <code>y</code>-axis. Imagine the tangent at the intersection. What is the slope?',
		"hint8"		: 'The tangent where the parabola intersects the <code>y</code>-axis was drawn in orange. The number <code>b</code> is the slope.',
		"hint9"		: "The tangent has a <var>(B > 0) ? 'positive' : ( (B < 0) ? 'negative' : 'zero' )</var> slope, so <code>b</code> is <var>(B > 0) ? 'positive' : ( (B < 0) ? 'negative' : 'zero' )</var>.",
		"hint10"	: "<code>a</code> is <var>( A > 0 ) ? 'positive' :  'negative'</var>, <code>b</code> is <var>(B > 0) ? 'positive' : ( (B < 0) ? 'negative' : 'zero' )</var>, and <code>c</code> is <var>(C > 0) ? 'positive' : ( (C < 0) ? 'negative' : 'zero' )</var>."
		}
})